# Go Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for / func
```go
if x > 0 { }
for i := 0; i < len(nums); i++ {
    v := nums[i]
    _ = v
}
for _, v := range nums {
    _ = v
}
func add(a, b int) int { return a + b }
```
