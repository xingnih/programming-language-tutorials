# Python Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for / function
```py
x = 3
if x > 0:
    pass
for i, v in enumerate(nums):
    pass

def add(a: int, b: int) -> int:
    return a + b
```

## list / string 常用
```py
len(nums)
nums.append(5)
nums[1:4]
"a,b".split(",")
"".join(["a","b"])
```

## debug
```py
print(nums)
```
