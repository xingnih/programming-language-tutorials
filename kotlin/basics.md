# Kotlin Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for / fun
```kt
if (x > 0) { }
for (i in nums.indices) {
  val v = nums[i]
}
for (v in nums) { }
fun add(a: Int, b: Int): Int = a + b
```
