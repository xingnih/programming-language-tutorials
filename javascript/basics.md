# JavaScript Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for / function
```js
let x = 3;
if (x > 0) {}
for (let i = 0; i < nums.length; i++) {
  const v = nums[i];
}
for (const v of nums) {}
```

## array / string 常用
```js
nums.push(5);
nums.slice(1, 4);
"a,b".split(",");
["a","b"].join("");
```
