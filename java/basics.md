# Java Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for
```java
int x = 3;
if (x > 0) {}
for (int i = 0; i < nums.length; i++) {
  int v = nums[i];
}
for (int v : nums) {}
```

## String
```java
String s = "a,b";
String[] parts = s.split(",");
int n = s.length();
```
