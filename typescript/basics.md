# TypeScript Basics（LeetCode 必用）

這份只收：你刷題會一直用到的語法（不講演算法）。

## if / for / function
```ts
let x = 3;
if (x > 0) {}
for (let i = 0; i < nums.length; i++) {
  const v = nums[i];
}
for (const v of nums) {}
```
